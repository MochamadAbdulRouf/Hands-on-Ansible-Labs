- name: Install tools for docker
  apt: 
    name: 
      - apt-transport-https
      - ca-certificates
      - curl
      - gnupg
    state: present

- name: Add Permission for Keyrings 
  file: 
    path: /etc/apt/keyrings
    state: directory
    mode: '0755'
    owner: root
    group: root

#  - name: Add Docker GPG key
 # apt_key: 
  # url: https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
  # state: present

- name: Download Docker GPG key 
  get_url:
    url: https://download.docker.com/linux/ubuntu/gpg
    dest: /etc/apt/keyrings/docker.asc
    mode: '0644'
    owner: root
    group: root 
    force: yes


- name: Add permission Docker Engine 
  file:
    path: /etc/apt/keyrings/docker.asc
    mode: '0644'
    state: file
    
- name: Add Docker Repository
  apt_repository:
    repo: "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"
    state: present
    filename: docker

- name: Install Docker Engine
  apt: 
    name: 
      - docker-ce 
      - docker-ce-cli
      - containerd.io
      - docker-buildx-plugin 
      - docker-compose-plugin

    state: present
    update_cache: yes

- name: Ensure Docker is running 
  service: 
    name: docker
    state: started 
    enabled: yes

#  After Ansible has finished running, execute this command.  
#  command : newgrp docker 